/**
 * Created by tabradford on 7/22/2015.
 */

var app = angular.module('rootControllers', ['ui.bootstrap']);

app.controller('TabCtrl',function($scope){

    window.location.hash = "#/main";

    $scope.changeHash = function(data) {
        window.location.hash = data;
    };

    $scope.tabs = [
        {title: 'Main', href:"#/main"},
        {title: 'Invoice', href:'#/invoice'},
        {title: 'Calculator', href:'#/calc'}
    ];
});

app.controller('InvoiceController', ['$scope','$routeParams', function($scope) {
    $scope.test = "InvoiceController is Working. This text was generated by the Angular $scope. Angular and routing are working...";
}]);

app.controller('MainController', ['$scope','$routeParams', function($scope) {
    $scope.test = "MainController is Working. This text was generated by the Angular $scope. Angular and routing are working...";
   // $scope.factor = myFactory.getName();
   // $scope.stuff = makeThings();
   // var vm = this;
   // vm.allInfo = info.getInfo();
}]);
/*
app.controller('CalcController', ['$scope','Calc','$routeParams', function($scope,Calc) {
    //$scope.test = "CalcController is Working. This text was generated by the Angular $scope. Angular and routing are working...";

    //Functions
    $scope.operationPress = function(theOperation, new_swtc) {
        $scope.result = Calc.operationPress(theOperation, new_swtc); //returns total
        $scope.calcNumber = Calc.getCalcNumber();

    };

    $scope.numberPress = function(theNumber) {
        $scope.calcNumber = Calc.numberPress(theNumber);//returns calcNumber

    };

    $scope.clearPress = function() {
        Calc.clearPress();
        $scope.result = 0;
        $scope.calcNumber = "";
    };

    //Variables
    $scope.calcNumber = Calc.getCalcNumber();
    $scope.result = Calc.getTotal();

}]);
//this file is going to be relocated to js/calcModule/calcController
*/

/*
app.factory('Calc', function(){
    var swtc = 0;
    var num = 0;
    var Total = 0;

    var calcNumber = "";

    function setNum(newNum) { num = newNum; }
    function setSwtc(newSwtc) { swtc = newSwtc; }

    function getTotal() { return Total; }
    function getCalcNumber() { return calcNumber; }

     function numberPress(theNumber) {
        num *= 10;
        num += theNumber;
        calcNumber += theNumber.toString();
         return calcNumber;
    }

    function operationPress(theOperation, new_swtc) {
        if(Total == 0){
            Total = num;
        }
        else{
            switch (swtc) {
                case 1:
                    Total *= num;// multiply
                    break;
                case 2:
                    Total /= num;// divide
                    break;
                case 3:
                    Total -= num;// minues
                    break;
                case 4:
                    Total += num;// plus
                    break;

                default:
                    break;
            }
        }
        calcNumber += theOperation;
        if(theOperation == "="){
            calcNumber += Total.toString();
        }
        swtc = new_swtc;
        num = 0;
        return Total;
    }

    function clearPress() {
        swtc = 0;
        num = 0;
        Total = 0;
        calcNumber = "";
        //$scope.result = 0;
        //$scope.calcNumber = "";
    }

    return {
        clearPress: clearPress,
        operationPress: operationPress,
        numberPress: numberPress,
        getTotal: getTotal,
        setSwtc: setSwtc,
        setNum: setNum,
        getCalcNumber: getCalcNumber
    }

});
 //this file is going to be relocated to js/calcModule/calcService
    */